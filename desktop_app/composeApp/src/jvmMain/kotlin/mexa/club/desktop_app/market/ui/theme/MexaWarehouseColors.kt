package mexa.club.desktop_app.market.ui.theme

import androidx.compose.ui.graphics.Color

object MexaWarehouseColors {

    // ── Surface / Background ──────────────────────────────────────────────────
    val surfaceLowest        = Color(0xFFFFFFFF)   // sof oq
    val surfaceContainerLow  = Color(0xFFF5F2FE)   // juda och binafsha
    val backgroundPage       = Color(0xFFF8F7FD)   // sahifa foni
    val tableHeaderBg        = Color(0xFFF1EFFA)   // jadval sarlavhasi
    val inputBg              = Color(0xFFF5F3FF)   // input maydon foni
    val rowHover             = Color(0xFFF5F3FF)   // hover qatori
    val cardHoverBg          = Color(0xFFF5F3FF)   // hover kartasi
    val cardHoverBorder      = Color(0xFFD8D5F5)   // hover kartasi borderi

    // ── Text ──────────────────────────────────────────────────────────────────
    val textPrimary          = Color(0xFF1B1B23)   // asosiy matn
    val textMuted            = Color(0xFF767586)   // ikkinchi darajali matn
    val textCaption          = Color(0xFF9896A8)   // caption
    val onSurface            = Color(0xFF1B1B23)
    val onSurfaceVariant     = Color(0xFF464554)
    val onSecondaryFixed     = Color(0xFF3F465C)

    // ── Border ────────────────────────────────────────────────────────────────
    val borderSubtle         = Color(0xFFE8E5F5)
    val outlineVariant       = Color(0xFFC7C4D7)
    val outline              = Color(0xFF767586)

    // ── Sidebar ───────────────────────────────────────────────────────────────
    val sidebarBg            = Color(0xFF131B2E)
    val sidebarNavMuted      = Color(0xFF8892A4)
    val sidebarSubtitle      = Color(0xFFC0C1FF)

    // ── Primary / Indigo ─────────────────────────────────────────────────────
    val primary              = Color(0xFF4648D4)
    val primaryContainer     = Color(0xFF6063EE)
    val primaryFixedDim      = Color(0xFFC0C1FF)
    val activeNavBg          = Color(0xFF4648D4).copy(alpha = 0.15f)

    val indigoAccent         = Color(0xFF6366F1)   // yorqin indigo
    val infoBadgeBg          = Color(0xFFEEF2FF)   // och indigo badge
    val statIndigoBg         = Color(0xFFEEF2FF)   // indigo stat kartasi foni

    // ── Status ────────────────────────────────────────────────────────────────
    val statusActiveBg       = Color(0xFFDCFCE7)
    val statusActiveFg       = Color(0xFF15803D)
    val statusBlockedBg      = Color(0xFFFFE4E6)
    val statusBlockedFg      = Color(0xFFB91C1C)

    // ── Stat cards ────────────────────────────────────────────────────────────
    val statCyanBg           = Color(0xFFECFEFF)
    val statCyanFg           = Color(0xFF0E7490)

    // ── Danger / Error ────────────────────────────────────────────────────────
    val danger               = Color(0xFFEF4444)
    val error                = Color(0xFFBA1A1A)
    val errorContainer       = Color(0xFFFFDAD6)

    // ── Role: SUPER_ADMIN ─────────────────────────────────────────────────────
    val roleSuperAdminBg     = Color(0xFF7C3AED)

    // ── Role: ADMIN ───────────────────────────────────────────────────────────
    val roleAdminBg          = Color(0xFFEEF2FF)
    val roleAdminFg          = Color(0xFF4648D4)
    val roleAdminBorder      = Color(0xFFC7D2FE)

    // ── Role: WAREHOUSE ───────────────────────────────────────────────────────
    val roleWarehouseBg      = Color(0xFFFEF3C7)
    val roleWarehouseFg      = Color(0xFFB45309)
    val roleWarehouseBorder  = Color(0xFFFDE68A)

    // ── Role: COURIER ─────────────────────────────────────────────────────────
    val roleCourierBg        = Color(0xFFECFEFF)
    val roleCourierFg        = Color(0xFF0E7490)
    val roleCourierBorder    = Color(0xFFA5F3FC)

    // ── Role: USER ────────────────────────────────────────────────────────────
    val roleUserBg           = Color(0xFFF1F5F9)
    val roleUserFg           = Color(0xFF475569)
    val roleUserBorder       = Color(0xFFCBD5E1)

    // ── Misc badges ───────────────────────────────────────────────────────────
    val tertiaryContainer    = Color(0xFFB55D00)
    val greenBadgeBg         = Color(0xFFDCFCE7)
    val greenBadgeFg         = Color(0xFF15803D)
    val blueBadgeBg          = Color(0xFFDBEAFE)
    val blueBadgeFg          = Color(0xFF1D4ED8)
    val amberBadgeBg         = Color(0xFFFEF3C7)
    val amberBadgeFg         = Color(0xFFB45309)
}
