package mexa.club.orderservice.client.payload;

/**
 * DEAD FILE — variants were removed from product-service. Each color is now its own
 * standalone Product, looked up directly via {@code ProductServiceClient#getProduct}
 * and {@code ProductServiceClient#getProductPrices}. This stub is kept only so any
 * stray import doesn't break a partial checkout; it is not referenced anywhere.
 */
@Deprecated
record ProductVariantResponse() {}
